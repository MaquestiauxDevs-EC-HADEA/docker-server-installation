{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "build": "npx tsc",
    "dev": "nodemon ./app/server.ts",
    "start": "tsc && node ./dist/server.js",
    "pm2-start": "npx tsc && pm2 start pm2.config.dev.js",
    "pm2-restart": "npx tsc && pm2 stop 0 && pm2 delete 0 && pm2 start pm2.config.dev.js",
    "pm2-stop": "pm2 stop 0 && pm2 delete 0",
    "pm2-logs": "pm2 logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.7",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}