{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "build": "rm -Rf dist && npx tsc && cp -r sources/public dist",
    "dev": "nodemon ./sources/server.ts",
    "start": "node ./dist/server.js",
    "start-and-build": "npm run build && node ./dist/server.js",
    "pm2-start": "npm run build && pm2 start ecosystem.dev.config.js",
    "pm2-restart": "npm run build && pm2 restart ecosystem.dev.config.js",
    "pm2-stop": "pm2 stop 0 && pm2 delete 0",
    "pm2-logs": "pm2 logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "oracledb": "^6.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.7",
    "@types/oracledb": "^6.0.4",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}